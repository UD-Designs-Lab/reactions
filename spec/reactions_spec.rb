# frozen_string_literal: true

RSpec.describe Reactions do
  it "has a version number" do
    expect(Reactions::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
